class Solution:
    def isPalindrome(self, x: int) -> bool:
        a=x
        if a<0:
            print ('false')
        else:
            reverse=0
            while (x>0):
                reminder=x%10
                reverse=(reverse*10)+reminder
                x=x//10
            if a==reverse:
                return 'true'
